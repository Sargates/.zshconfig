{
	"create_cs": "no",			// Ensure `$HOME/dev` folder exists
	"link_tmuxconf": "no",		// Link ohmytmux files in `$ZDOTDIR/config`
	"link_gitconf": "no"		// Link `$HOME/.gitconfig` to `$ZDOTDIR/config/.gitconfig`
}
